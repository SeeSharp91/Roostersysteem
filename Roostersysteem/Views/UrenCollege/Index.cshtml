@model IEnumerable<Roostersysteem.Models.UrenCollege>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CollegeNaam)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CollegeNaam)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.UrenCollegeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.UrenCollegeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.UrenCollegeId })
        </td>
    </tr>
}

</table>
