@model IEnumerable<Roostersysteem.Models.PersoonUren>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Persoon.PersoonVoornaam)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UrenBeschikbaarheid.DagBeschikbaarheid)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Persoon.PersoonVoornaam)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UrenBeschikbaarheid.DagBeschikbaarheid)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PersoonUren_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.PersoonUren_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PersoonUren_Id })
        </td>
    </tr>
}

</table>
